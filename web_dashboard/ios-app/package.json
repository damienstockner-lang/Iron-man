{
  "name": "friday-assistant-ios",
  "version": "1.0.0",
  "description": "Friday Assistant iOS App",
  "main": "index.js",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "build": "cap copy ios && cd ios/App && xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -archivePath build/App.xcarchive archive",
    "build:ipa": "cap copy ios && cd ios/App && xcodebuild -exportArchive -archivePath build/App.xcarchive -exportOptionsPlist ExportOptions.plist -exportPath build/",
    "setup": "npm install && npm run sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/haptics": "^5.0.0",
    "@capacitor/keyboard": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
